import { create, NButton, NMenu, NInput, NMessageProvider,NAffix } from "naive-ui";

const naive = create({
  components: [NButton, NInput, NMenu, NMessageProvider,NAffix],
});

export default naive;
